// Registration Types - อัปเดตให้ใช้ types ใหม่
// Re-export จาก files ใหม่เพื่อ backward compatibility

export type { 
  SessionRegistrationData 
} from './customer-session';

export type { 
  DurationRegistrationData 
} from './customer-duration';